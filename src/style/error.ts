import styled from 'styled-components';
import { SUIT_FONT_NAME } from './global';
import { MiddleContainer } from './position';

export const ErrorContainer = styled(MiddleContainer)`
    margin-top: 15%;
    font-family: ${SUIT_FONT_NAME};
    width: auto;
    height: auto;
    text-align: center;
`;
